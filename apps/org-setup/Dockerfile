# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build org-setup`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t org-setup`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system org-setup && \
          adduser --system -G org-setup org-setup

COPY dist/apps/org-setup org-setup
RUN chown -R org-setup:org-setup .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix org-setup --omit=dev -f install

CMD [ "node", "org-setup" ]
